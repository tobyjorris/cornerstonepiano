import React from 'react';
import './footer.css';
import {Container} from "react-bootstrap";

const footer = () => {
    return (
        <Container style={{background: 'black', height: '40px'}} fluid>

        </Container>
    )

}

export default footer;
